package patterns.structuralPatterns.composite;

public interface LexicEntity { //component
    void print(); //writes object as composition of subobject text classes
    int count(); //counts subobject text classes
}
